import styled from "styled-components"
import {H2, GridLayout} from "../../styles/global"

export const Heading2 = styled.h2`
    ${H2}
    margin-bottom: 48px; 
`

export const Wrapper = styled.section`
${GridLayout}
`